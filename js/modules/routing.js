import './routie';

// Routie to ID
  routie( {
    ':id': function() {
      let detailpage = document.getElementById('detailpage');
      detailpage.appendChild(p); 
      detailpage.appendChild(int);
    },
    
  })